pipeline{
    agent any
    stages {
        stage("Clone") {
            steps{
                echo "======== Executing clone git repo ========"
                git "https://github.com/supertai22298/jenkins-file.git"
                echo "========Finish executing clone git repo ========"
            }
        }
    }
}
